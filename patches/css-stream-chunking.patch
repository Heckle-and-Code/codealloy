diff --git a/src/vs/platform/cssDev/node/cssDevService.ts b/src/vs/platform/cssDev/node/cssDevService.ts
index 5aa1efa..d2f8ff1 100644
--- a/src/vs/platform/cssDev/node/cssDevService.ts
+++ b/src/vs/platform/cssDev/node/cssDevService.ts
@@ -49,21 +49,21 @@ export class CSSDevelopmentService implements ICSSDevelopmentService {
 
 			const sw = StopWatch.create();
 
-			const chunks: string[][] = [];
+			let stdoutText = '';
 			const decoder = new TextDecoder();
 			const basePath = FileAccess.asFileUri('').fsPath;
 			const process = spawn(rg.rgPath, ['-g', '**/*.css', '--files', '--no-ignore', basePath], {});
 
 			process.stdout.on('data', data => {
-				const chunk = decoder.decode(data, { stream: true });
-				chunks.push(chunk.split('\n').filter(Boolean));
+				stdoutText += decoder.decode(data, { stream: true });
 			});
 			process.on('error', err => {
 				this.logService.error('[CSS_DEV] FAILED to compute CSS data', err);
 				resolve([]);
 			});
 			process.on('close', () => {
-				const result = chunks.flat().map(path => relative(basePath, path).replace(/\\/g, '/')).filter(Boolean).sort();
+				stdoutText += decoder.decode();
+				const result = stdoutText.split('\n').map(l => l.trim()).filter(Boolean).map(path => relative(basePath, path).replace(/\\/g, '/')).filter(Boolean).sort();
 				resolve(result);
 				this.logService.info(`[CSS_DEV] DONE, ${result.length} css modules (${Math.round(sw.elapsed())}ms)`);
 			});
